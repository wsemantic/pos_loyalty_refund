<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="pos_loyalty_refund.PaymentScreenValidate" t-inherit="point_of_sale.PaymentScreenValidate" t-inherit-mode="extension">
        <xpath expr="//button[hasclass('validation-button') and hasclass('next')]" position="attributes">
            <attribute name="t-att-hidden">pos.config.set_tip_after_payment and !currentOrder.is_paid()</attribute>
        </xpath>

        <xpath expr="//button[hasclass('validation-button') and hasclass('next')]" position="replace">
            <div style="
                display: flex;
                flex-direction: row;
                width: 100%;
                border: none;
                max-height: 100%;"
            >
            <button class="validation-button next btn btn-primary btn-lg py-3 lh-lg mx-2"
                    t-on-click="() => this.validateOrder()"
                    t-attf-class="
                        {{ ui.isSmall ? 'btn-switchpane flex-fill' : 'button next w-50' }}
                        {{ currentOrder.canBeValidated() ? 'highlight' : 'disabled' }}
                    "
                >
             <span>Validar</span>
            </button>
             <button class="validation-button next btn btn-primary btn-lg py-3 lh-lg"
                    t-att-hidden="pos.config.iface_print_auto"
                    t-on-click="() => this.validateOrder()"
                    t-attf-class="
                        {{ ui.isSmall ? 'btn-switchpane flex-fill' : 'button next w-50' }}
                        {{ currentOrder.canBeValidated() ? 'highlight' : 'disabled' }}
                    "
                >
             <span>Sin Precio</span>
            </button>
        </div>
        </xpath>
    </t>

</templates>
